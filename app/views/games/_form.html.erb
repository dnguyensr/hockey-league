<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :team_game_id %><br>
    <%= f.number_field :team_game_id %>
  </div>
  <div class="field">
    <%= f.label :game_date %><br>
    <%= f.date_select :game_date %>
  </div>
  <div class="field">
    <%= f.label :game_time %><br>
    <%= f.time_select :game_time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
